// 🐨 Create a variable `age` with a number
// 💰 const age = 21

// 🐨 Use the ternary operator to set `canVote` to "Yes" if age >= 18, otherwise "No"
// 💰 const canVote = age >= 18 ? "Yes" : "No"

// 🐨 Create a variable `score` with a number

// 🐨 Use the ternary operator to set `status` to "Pass" if score >= 60, otherwise "Fail"

// 🐨 Log both results
// 💰 console.log('Can vote:', canVote)
