// 🐨 Create `double` as an arrow function that takes a number and returns it doubled
// 💰 const double = (n) => n * 2

// 🐨 Create `greet` as an arrow function that takes a name and returns "Hello, [name]!"
// 💰 const greet = (name) => "Hello, " + name + "!"

// 🐨 Create `isPositive` as an arrow function that returns true if number > 0
// 💰 const isPositive = (n) => n > 0

// 🐨 Call each function and log the results
// 💰 console.log(double(5))
