# Ternary Operator

👨‍💼 For simple either/or decisions, the ternary operator provides a compact
one-line syntax.

The syntax:

```js
condition ? valueIfTrue : valueIfFalse
```

This is equivalent to:

```js
let result
if (condition) {
	result = valueIfTrue
} else {
	result = valueIfFalse
}
```

🐨 Open <InlineFile file="index.js" /> and:

1. Create a variable `age` with a number
2. Use the ternary operator to set `canVote` to `"Yes"` if age >= 18, otherwise
   `"No"`
3. Create a variable `score` with a number
4. Use the ternary operator to set `status` to `"Pass"` if score >= 60,
   otherwise `"Fail"`
5. Log both results

💰 Example:

```js
const message = isHappy ? 'Smile!' : 'Cheer up!'
```
