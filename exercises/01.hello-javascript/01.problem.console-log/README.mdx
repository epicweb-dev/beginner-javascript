# console.log

👨‍💼 We need a way to see what our JavaScript code is doing. The `console.log()`
function lets us output values so we can see them.

🐨 Open <InlineFile file="index.js" /> and use `console.log()` to output the
message "Hello, JavaScript!".

💰 The syntax looks like this:

```js
console.log('Your message here')
```

When you run the code, you should see "Hello, JavaScript!" appear in the output.

📜 [console.log() on MDN](https://developer.mozilla.org/en-US/docs/Web/API/console/log_static)
