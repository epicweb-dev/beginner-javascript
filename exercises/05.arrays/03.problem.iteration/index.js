// 🐨 Create an array `numbers` with values [1, 2, 3, 4, 5]
// 💰 const numbers = [1, 2, 3, 4, 5]

// 🐨 Use `forEach` to log each number multiplied by 2
// 💰 numbers.forEach((num) => console.log(num * 2))

// 🐨 Use `map` to create a new array `doubled` where each number is doubled
// 💰 const doubled = numbers.map((num) => num * 2)

// 🐨 Use `filter` to create a new array `evens` with only even numbers
// 💰 const evens = numbers.filter((num) => num % 2 === 0)

// 🐨 Log both `doubled` and `evens`
// 💰 console.log("Doubled:", doubled)
// 💰 console.log("Evens:", evens)
